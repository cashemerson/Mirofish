# Mirofish

Always-on deployment resources are available in:

- `deploy/always-on/README.md`

This includes:

- Docker/VPS `docker-compose.yml` (recommended)
- Render `render.yaml`
- Railway `railway.json`
- Lightweight launcher website in `portal/` (optional)
